import 'dotenv/config'
import { DEV_ENV } from '~/utils/constants'

const {
  NODE_ENV,
  MONGODB_URI,
  DATABASE_NAME,
  APP_PROTOCOL,
  APP_HOST,
  APP_PORT,
  BRAND_NAME,
  CLIENT_URL,
  CLIENT_OTP_FORM_PATH,
  ACCESS_TOKEN_EXPIRES,
  REFRESH_TOKEN_EXPIRES,
  PASSWORD_RESET_OTP_TIME,
  NEW_PASSWORD_NOT_SAME_OLD_PASSWORD_TIME,
  EMAIL_VERIFICATION_TOKEN_TIME,
  EMAIL_APP_PASSWORD,
  EMAIL_NAME,
  CLOUDINARY_NAME,
  CLOUDINARY_API_KEY,
  CLOUDINARY_API_SECRET,
  PASSWORD_RESET_OTP_BASE32_SECRET_KEY,
  GHN_TOKEN
} = process.env

export const BUILD_MODE = NODE_ENV || DEV_ENV

export const APP = {
  PROTOCOL: APP_PROTOCOL || 'http',
  HOST: APP_HOST || 'localhost',
  PORT: APP_PORT || 5600,
  BRAND_NAME: BRAND_NAME || 'No Name Yet'
}

export const CLIENT = {
  URL: CLIENT_URL || 'http://localhost:3000',
  CLIENT_OTP_FORM_PATH: CLIENT_OTP_FORM_PATH || '/auth/password-reset-otp'
}

export const MONGODB = {
  URI: MONGODB_URI || 'mongodb://localhost:27017/',
  DATABASE_NAME: DATABASE_NAME || ''
}

export const AUTH = {
  ACCESS_TOKEN_EXPIRES: ACCESS_TOKEN_EXPIRES || 1000 * 60 * 60,
  REFRESH_TOKEN_EXPIRES: REFRESH_TOKEN_EXPIRES || 1000 * 60 * 60 * 24 * 90,
  PASSWORD_RESET_OTP_TIME: +PASSWORD_RESET_OTP_TIME || 1000 * 60 * 15,
  EMAIL_VERIFICATION_TOKEN_TIME:
    +EMAIL_VERIFICATION_TOKEN_TIME || 1000 * 60 * 15,
  NEW_PASSWORD_NOT_SAME_OLD_PASSWORD_TIME:
    +NEW_PASSWORD_NOT_SAME_OLD_PASSWORD_TIME || 1000 * 60 * 60 * 24 * 90,
  PASSWORD_RESET_OTP_SECRET_KEY: PASSWORD_RESET_OTP_BASE32_SECRET_KEY || ''
}

export const EMAIL = {
  APP_PASSWORD: EMAIL_APP_PASSWORD,
  NAME: EMAIL_NAME
}

export const CLOUDINARY = {
  NAME: CLOUDINARY_NAME,
  API_KEY: CLOUDINARY_API_KEY,
  API_SECRET: CLOUDINARY_API_SECRET
}

export const PARTNERS = {
  GHN: {
    TOKEN: GHN_TOKEN
  }
}