import 'dotenv/config'
import { DEV_ENV } from '~/utils/constants'

const {
  NODE_ENV,
  MONGODB_URI,
  DATABASE_NAME,
  APP_PROTOCOL,
  APP_HOST,
  APP_PORT,
  ACCESS_TOKEN_EXPIRES,
  REFRESH_TOKEN_EXPIRES,
  PASSWORD_RESET_TOKEN_EXPIRES,
  EMAIL_APP_PASSWORD,
  EMAIL_NAME,
  CLOUDINARY_NAME,
  CLOUDINARY_API_KEY,
  CLOUDINARY_API_SECRET
} = process.env

export const BUILD_MODE = NODE_ENV || DEV_ENV

export const APP = {
  PROTOCOL: APP_PROTOCOL || 'http',
  HOST: APP_HOST || 'localhost',
  PORT: APP_PORT || 5600
}

export const MONGODB = {
  URI: MONGODB_URI || 'mongodb://localhost:27017/',
  DATABASE_NAME: DATABASE_NAME || ''
}

export const AUTH = {
  ACCESS_TOKEN_EXPIRES: ACCESS_TOKEN_EXPIRES || 1000 * 60 * 60,
  REFRESH_TOKEN_EXPIRES: REFRESH_TOKEN_EXPIRES || 1000 * 60 * 60 * 24 * 90,
  PASSWORD_RESET_TOKEN_EXPIRES: PASSWORD_RESET_TOKEN_EXPIRES || 1000 * 60 * 15
}

export const EMAIL = {
  APP_PASSWORD: EMAIL_APP_PASSWORD,
  NAME: EMAIL_NAME
}

export const CLOUDINARY = {
  NAME: CLOUDINARY_NAME,
  API_KEY: CLOUDINARY_API_KEY,
  API_SECRET: CLOUDINARY_API_SECRET
}
